{% extends 'Admin/admin_base.html.twig' %}

{% block title %}Hello OrderController!{% endblock %}

{% block body %}
    <div class="container tableau">
        <div class="row gy-3">
            <div class="col-12">
                <h1>Toutes les commandes</h1>
            </div>
            <div class="col-12">
                <table class="table table-hover table-striped text-center">
                    <thead>
                    <tr>
                        <th>id</th>
                        <th>Date de création</th>
                        <th>Date de fin</th>
                        <th>Statut</th>
                        <th>Total</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for order in pagination %}
                        {#                        <div>#}
                        <tr>
                            <td>{{ order.id }}</td>
                            <td>{{ order.createdAt|date('d-m-Y h:i:s') }}</td>
                            <td>{{ order.endedAt|date('d-m-Y h:i:s') }}</td>
                            <td>{{ order.status }}</td>
                            <td></td>
                            <td>
                                <a href="" class="btn btn-primary me-2"><i class="fas fa-eye"></i></a>
                                <a class="btn btn-primary" href="{{ path('admin_user_index') }}"><i class="fas fa-user"></i></a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            <div class="col-12 justify-content-center d-flex">
                <div class="pagination">
                    {{ knp_pagination_render(pagination) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
