{% extends 'Front/front_base.html.twig' %}

{% block title %}Hello OrderController!{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1 class="my-3">Panier de {{ order.user.nickName }}</h1>
        </div>
        <div class="col-12">
            <div class="row gy-2">
                {% for orderLine in order.orderLines %}
                    <div class="col-12">
                        {% include 'partials/front/_order_line.html.twig' %}
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
